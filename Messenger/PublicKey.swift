import Foundation
import ObjectMapper

/*
class PublicKey: Mappable {
    
    var id: String?
    var algo: String?
    var value: String?
    
    init() {
    }
    
    required init?(_ map: Map) {
    }
    
    func mapping(map: Map) {
        id <- map["id"]
        algo <- map["algo"]
        value <- map["value"]
    }
}
 */
